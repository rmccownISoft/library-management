# Database Configuration
DATABASE_URL="file:./prisma/dev.db"

# Production Database URL (for Docker deployment)
# DATABASE_URL="file:/app/data/library.db"

# Application Configuration
NODE_ENV="development"
PORT=3000
HOST="0.0.0.0"

# Optional: Origin URL (set in production)
# ORIGIN="http://localhost:3000"

# File Upload Settings
# Base path for file uploads (images, documents, etc.)
# In production with Docker, set this to: /app/uploads
# In development, defaults to C:/Temp (Windows) or uses this value
UPLOAD_BASE_PATH="./uploads"

# Add any other environment variables your application needs
