# Auto-approve build scripts for specific packages
# This allows sharp to build its native binaries during install
enable-pre-post-scripts=true

# Specifically approve sharp's build scripts
# Sharp needs to compile native binaries for image processing
public-hoist-pattern[]=*sharp*
